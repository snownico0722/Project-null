# Changelog

## 0.91

- 沿 DOM 祖先链合并同一轮变化中的嵌套扫描根，避免平方级比较，同时保持局部扫描范围。
- 忽略脚本自己写入的文本变化，同时继续响应页面产生的外部文本变化。
- 在单轮扫描中复用可见性、头像外观、提及正则和假名分配结果。
- 将局部视觉观察器清理从“每个子树一次”改为“每批一次”。
- 身份节点已有直接用户名时，不再额外搜索后代头像。
- 修复假名桶有符号下标导致避碰失效并错误选中兜底词的问题。
- 新增离线自动回归，覆盖屏蔽、恢复、节点复用、纯净模式、身份作用域、周期重置、
  假名提及、设置写入、独立帖子并发变化与扫描工作量上限。
